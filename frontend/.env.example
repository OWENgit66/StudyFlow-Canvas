# Blank uses the same-origin /api proxy. Only a public API origin belongs here.
NEXT_PUBLIC_API_BASE_URL=
# Next.js server-side proxy destination (not a browser variable).
BACKEND_API_URL=http://127.0.0.1:8000
